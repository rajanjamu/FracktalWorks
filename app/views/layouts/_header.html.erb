<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to("Fracktal Works", root_path, class: "navbar-brand") %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><%=content_tag(:span, "", class: "glyphicon glyphicon-user") + current_user.email %> <span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
              <%= content_tag(:li) do %>
                <%= link_to content_tag(:span, "", class: "glyphicon glyphicon-download-alt") + "New Order", new_order_path %>
              <% end %>
              <li class="divider"></li>
              <%= content_tag(:li) do %>
                <%= link_to content_tag(:span, "", class: "glyphicon glyphicon-log-out") + "Logout", destroy_user_session_path, :method => :delete %>
              <% end %>
            </ul>
          </li>
        <% else %>
          <li><%= link_to content_tag(:span, "", class: "glyphicon glyphicon glyphicon-log-in") + "Login", new_user_session_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>